
[pytest]
norecursedirs = .git .venv venv build deps tests/deps node_modules package
addopts = -v --tb=long

testpaths = integration modinput_functional

filterwarnings =
    ignore::DeprecationWarning
